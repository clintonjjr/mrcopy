{
  "name": "mrcopy",
  "version": "0.2.0",
  "description": "Signal/call agent: watches smart perp traders on Hyperliquid + Aster, emits structured calls for your Binance Agent OS execution agent. Never holds Binance keys, never places orders.",
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json && mkdir -p dist/soul && cp src/soul/SOUL.md dist/soul/SOUL.md",
    "start": "node dist/index.js",
    "mcp": "tsx src/mcp-standalone.ts",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "better-sqlite3": "^11.10.0",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/express": "^4.17.21",
    "@types/node": "^22.7.0",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2"
  }
}
